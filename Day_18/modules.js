export let msg = "Namaskaram, bagunnara";
 export function greet(){
    return msg+"I am coding"
}

export default greet;msg;
//create a form with username(!empty),eamil(@,!empty) and password (should be 6)  and confirm passwords (same,!empty),once you 
//  click on submit button, a alert should be popped up on you screen "form submitted successfully" if and only if all the details are correct
